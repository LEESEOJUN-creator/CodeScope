# 백엔드 API 오리진. 브라우저에서 직접 사용되므로(로그인 버튼 링크 + fetch 호출)
# NEXT_PUBLIC_ 접두사가 필요하다(Next.js는 이 접두사가 있는 변수만 클라이언트
# 번들에 포함시킨다). 실제 값은 .env.local에 복사해서 사용 (gitignore 대상).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
